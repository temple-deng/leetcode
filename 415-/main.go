package main

func addStrings(num1 string, num2 string) string {
	n1 := len(num1)
	n2 := len(num2)
	result := ""
	carrier := 0

	length := n1
	if n2 > n1 {
		length = n2
	}

	for i := 0; i < length; i++ {
		
	}
}